import { StyleSheet } from "react-native"

export default StyleSheet.create({
    title: {
        color: "#323C50",
        fontWeight: "bold",
        textAlign: "center",
        fontSize: 32
    }
})